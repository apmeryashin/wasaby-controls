<div class="root {{(_options.theme.indexOf('default') !== 0 || _options.theme.indexOf('light') !== 0) ? ' root__dark_background'}}">
    <div class="separator">Ни одной выбранной вкладки</div>
    <div class="demo-TabButtons ControlsDemo-Tabs__custom">
        <Controls.tabs:Buttons
                bind:selectedKey='SelectedKey'
                keyProperty="id"
                items="{{_items}}"
        />
    </div>
</div>
